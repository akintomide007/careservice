# Docker Environment Configuration
# This file contains environment variables for Docker deployment

# Node Environment
NODE_ENV=development

# API Configuration
PORT=3001
API_BASE_URL=http://localhost:3001

# Database (using Docker service name)
DATABASE_URL=postgresql://careuser:carepass@postgres:5432/care_provider_db

# Redis (using Docker service name)
REDIS_URL=redis://redis:6379

# JWT Configuration
JWT_SECRET=your-local-dev-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Storage Configuration
STORAGE_TYPE=local
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Microsoft 365 Integration (Mock mode for development)
USE_MOCK_MS365=true
MOCK_DELAY_MS=500

# Azure AD (Optional - for production)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
AZURE_AD_REDIRECT_URI=http://localhost:3001/auth/callback

# Microsoft Graph API
GRAPH_API_BASE=https://graph.microsoft.com/v1.0
SHAREPOINT_SITE_URL=

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# Next.js Dashboard Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001
